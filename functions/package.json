{
  "name": "ileotaku-functions",
  "description": "ÍléOtaku Cloud Functions — push notification delivery",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "private": true
}
